name: Tests

on:
  push:
    branches: [ "master" ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17.2
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d test_db"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DJANGO_SETTINGS_MODULE: bloggies.settings.settings_tests
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: test_db
      DB_HOST: localhost
      DB_PORT: 5432

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: 📦 Install Pipenv
        run: |
          python -m pip install --upgrade pip
          pip install pipenv

      - name: 📦 Install Dependencies
        env:
          PIPENV_IGNORE_VIRTUALENVS: "1"
        run: |
          pipenv install --dev --deploy

      - name: 🧪 Run tests
        env:
          PIPENV_IGNORE_VIRTUALENVS: "1"
        run: |
          pipenv run python manage.py test
